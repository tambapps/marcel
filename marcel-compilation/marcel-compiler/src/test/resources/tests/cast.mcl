import static org.junit.jupiter.api.Assertions.assertEquals

int i = 1
Integer I = cast<int>(i)
long l = cast<long>(i)

assertEquals(I, Integer.valueOf(i))
assertEquals(l, cast<long>(i))

Object oir = 1..2
IntRange ir = cast<IntRange>(oir)
assertEquals(ir, oir)